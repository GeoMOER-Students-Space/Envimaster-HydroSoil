Repo HydroSoil

Prepare -> create new folder in junction local path called "Envimaster-HydroSoil/repo"
Init -> clone Repo to local path junction "Envimaster-HydroSoil/repo"
Init -> head script change pathdir = "src/" to pathdir = "repo/src/" and let it run
folder structure shoud now be in the correct order

run src/head_hydro_v1 (setup folder str)